# task-11 — EJS ile Kullanıcı Listesi

## Hızlı Başlangıç
```bash
npm install
npm run dev   # veya: npm start
# http://localhost:3000
```

- `GET /`     ⇒ EJS ile liste görünümü
- `GET /api/users` ⇒ JSON olarak aynı veriler
